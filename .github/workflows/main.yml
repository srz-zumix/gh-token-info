name: Test
on:
  pull_request:

permissions: read-all

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: make install
      - run: gh token-info ${{ secrets.GITHUB_TOKEN }}
